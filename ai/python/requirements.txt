# CPU-only is plenty for a model this size; drop the index-url line for CUDA.
# pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.2
numpy>=1.24

# Optional — only for `python -m mtgai.export_onnx`:
# onnx
# onnxscript
